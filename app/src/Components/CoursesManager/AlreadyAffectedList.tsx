import React, {useEffect} from 'react';

function AlreadyAffectedList(props) {

    useEffect(() => {

    }, []);

    return (
        <div>{props.course_id}</div>
    );
}

export default AlreadyAffectedList;